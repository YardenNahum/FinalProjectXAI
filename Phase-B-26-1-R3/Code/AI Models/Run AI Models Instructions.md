# Run AI Models Instructions

To run the AI models, open the relevant notebook file from the project repository.

After opening the notebook, click:

```text
Open in Colab
```

Then, in Google Colab, click:

```text
Runtime → Run all
```

This will execute the full model pipeline, including preprocessing, model training, prediction generation, XAI explanation generation, and saving the results for the web system.


